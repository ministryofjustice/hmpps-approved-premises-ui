import { Section } from '../../../utils/decorators'

import PreArrivalContactResident from './contact-resident'

@Section({
  title: 'Contact resident',
  tasks: [PreArrivalContactResident],
})
export default class ContactResident {}
