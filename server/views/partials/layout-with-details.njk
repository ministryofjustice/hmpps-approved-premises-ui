{% from "components/keyDetails/macro.njk" import keyDetails %}
{% extends "partials/layout.njk" %}

{% block header %}
    {{ super() }}

    {% if contextKeyDetails %}
        {{ keyDetails(contextKeyDetails) }}
    {% endif %}
{% endblock %}
