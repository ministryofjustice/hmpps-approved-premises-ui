{% from "govuk/components/back-link/macro.njk" import govukBackLink %}
{% from "govuk/components/details/macro.njk" import govukDetails %}

{% extends "partials/layout.njk" %}

{% set pageTitle = applicationName + " - " + premises.name %}
{% set mainClasses = "app-container govuk-body" %}

{% block beforeContent %}
    {{ govukBackLink({
        text: "Back",
        href: backlink
    }) }}
{% endblock %}

{% block content %}
    {% include "_messages.njk" %}

    <span class="govuk-caption-l">{{ premises.name }}</span>
    <h1 class="govuk-heading-l">Local restrictions</h1>

    {{ govukDetails({
        summaryText: 'View criteria for this Approved Premises',
        html: 'Premises criteria TBD'
    }) }}

    {% if not restrictions | length %}
        <p>This AP has no local restrictions.</p>
    {% endif %}

{% endblock %}
