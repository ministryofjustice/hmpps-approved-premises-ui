import { path } from 'static-path'

const paths = {
  pages: {
    accessibilityStatement: path('/accessibility'),
    maintenance: path('/maintenance'),
    privacyNotice: path('/privacy')
  },
}

export default paths
