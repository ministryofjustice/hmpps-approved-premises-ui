---
# Per environment values which override defaults in hmpps-community-payback-ui/values.yaml

generic-service:
  replicaCount: 2

  ingress:
    host: community-payback-test.hmpps.service.justice.gov.uk
    tlsSecretName: hmpps-community-payback-test-cert

  env:
    INGRESS_URL: "https://community-payback-test.hmpps.service.justice.gov.uk"
    HMPPS_AUTH_URL: "https://sign-in-dev.hmpps.service.justice.gov.uk/auth"
    TOKEN_VERIFICATION_API_URL: "https://token-verification-api-dev.prison.service.justice.gov.uk"
    COMMUNITY_PAYBACK_API_URL: 'https://community-payback-api-test.hmpps.service.justice.gov.uk'
    ENVIRONMENT_NAME: TEST
    AUDIT_ENABLED: "false"

generic-prometheus-alerts:
  alertSeverity: hmpps-community-payback-nonprod
